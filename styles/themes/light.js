import colors from '../colors';
import base from './base';

export default {
  ...base,
  name: 'light',
  text: colors.deepPurple,
  background: colors.white,
  accent: colors.neonMagenta,
  secondary: colors.neonCyan,
  gradient: colors.prismatic,
  cursor: colors.neonMagenta,
};
