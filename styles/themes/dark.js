import colors from '../colors';
import base from './base';

export default {
  ...base,
  name: 'dark',
  text: colors.white,
  background: colors.cosmicBlue,
  accent: colors.neonCyan,
  secondary: colors.neonMagenta,
  gradient: colors.holoPrimary,
  cursor: colors.neonCyan,
};
